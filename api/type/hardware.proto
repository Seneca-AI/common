syntax = "proto3";

option go_package ="seneca/api/types";

// key(id)
message Camera {
    string id = 1;
    // The vehicle this camera is associated with at this current time.
    string vehicle_id = 2;
}