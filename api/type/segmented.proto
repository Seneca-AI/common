syntax = "proto3";

// import "common.proto";

option go_package ="seneca/api/types";

// key(user_id, id)
message SegmentedTrip {
    string user_id = 1;
    string id = 2;
    // TimePeriod time_period = 3;
    repeated string cut_video_ids = 4;
    repeated string raw_location_ids = 5;
    repeated string raw_motion_ids = 6;
}